命令行输入
具体格式
1.maxsum.exe  <file name>       读取file内容，为求出二维数组最小子数组
2.maxsum.exe  /v <file name>    水平环
3.maxsum.exe  /h <file name>    垂直环
4.maxsum.exe  /v /h <file name> 备胎！
5.maxsum.exe  /a <file name>
其中，1由maxSum_1函数完成，2、3、4由maxSum_2函数完成，5由maxSum_3完成。

输入在main函数中体现
文件格式如要求中所提到